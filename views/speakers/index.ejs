<%- include('../header.ejs', {name: 'Speakers Index'}) %>

<h1 class="title">Speakers</h1>
<br>
<% for(let i= 0; i < items.length; i++) { %>
    <div class="indexDiv">
    <a href="/speakers/<%= items[i]._id %>"<h3><%= items[i].name %></h3>
    <img src="<%= items[i].image %>" class="indeximg"></a>
    <p class="indexPrice"><%= items[i].price %></p>
    <p class="indexDescription"><%= items[i].description %></p>
    </div>
    <% } %>

<%- include('../footer.ejs') %>